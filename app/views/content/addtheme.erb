<h1>Add</h1>
<p><%= @theme.text %></p>


<%= form_for(:head_line, :url => {:action => 'tadd', :theme=> @theme.id},:html =>{:multipart => true}) do |f| %>
  <%= f.text_field(:text) %>
     <div class="form-buttons">
      <%= submit_tag('Create comment') %>
    </div>

  <% end %>